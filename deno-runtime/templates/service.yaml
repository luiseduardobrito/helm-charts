apiVersion: v1
kind: Service
metadata:
  name: {{ include "deno-runtime.fullname" . }}
  labels:
    {{- include "deno-runtime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deno-runtime.selectorLabels" . | nindent 4 }}
